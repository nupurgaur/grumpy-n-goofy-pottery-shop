import * as React from "react"

// Minimal sheet component to fix sidebar dependency
export const Sheet = (props: any) => <div {...props} />
export const SheetContent = (props: any) => <div {...props} />